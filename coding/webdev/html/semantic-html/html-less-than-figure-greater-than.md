---
description: it figures that we have images and captions!
---

# HTML \<figure>

A `<figure>` would contain media (usually an image) and a `<figcaption>` to describe that media:

```markup
<figure>
    <img src="..." alt="">
    <figcaption>A caption for the image, of course!</figcaption>
</figure>
```
